<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Pedido de venta -->
    <record id="sale_order_form_whatsapp" model="ir.ui.view">
        <field name="name">sale.order.form.whatsapp</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="%(action_whatsapp_compose)d" type="action"
                        string="WhatsApp" icon="fa-whatsapp"/>
            </xpath>
        </field>
    </record>

    <!-- Pedido de compra -->
    <record id="purchase_order_form_whatsapp" model="ir.ui.view">
        <field name="name">purchase.order.form.whatsapp</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="%(action_whatsapp_compose)d" type="action"
                        string="WhatsApp" icon="fa-whatsapp"/>
            </xpath>
        </field>
    </record>

    <!-- Factura / documento contable -->
    <record id="account_move_form_whatsapp" model="ir.ui.view">
        <field name="name">account.move.form.whatsapp</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="%(action_whatsapp_compose)d" type="action"
                        string="WhatsApp" icon="fa-whatsapp"/>
            </xpath>
        </field>
    </record>

    <!-- Remito / albarán -->
    <record id="stock_picking_form_whatsapp" model="ir.ui.view">
        <field name="name">stock.picking.form.whatsapp</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="%(action_whatsapp_compose)d" type="action"
                        string="WhatsApp" icon="fa-whatsapp"/>
            </xpath>
        </field>
    </record>
</odoo>
