<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Odoo 19: la categoria de permisos se modela con res.groups.privilege -->
    <record id="privilege_trx_sale" model="res.groups.privilege">
        <field name="name">Ventas Trixocom</field>
        <field name="category_id" ref="base.module_category_sales_sales"/>
        <field name="sequence">90</field>
    </record>

    <!-- Miembros de este grupo SI pueden crear productos desde la linea de venta -->
    <record id="group_sale_product_create" model="res.groups">
        <field name="name">Crear productos desde la linea de pedido</field>
        <field name="privilege_id" ref="privilege_trx_sale"/>
        <field name="comment">Los usuarios con este permiso pueden crear productos nuevos directamente desde el buscador de la linea del pedido de venta (opciones "Crear" y "Crear y editar"). Los usuarios sin este permiso solo pueden seleccionar productos ya existentes.</field>
    </record>
</odoo>
