<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Vista de formulario de orden de venta con campo de escaneo -->
    <record id="view_order_form_barcode_scanner" model="ir.ui.view">
        <field name="name">sale.order.form.barcode.scanner</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            
            <!-- Agregar campo de escaneo antes de las líneas de orden -->
            <xpath expr="//field[@name='order_line']" position="before">
                <group>
                    <group>
                        <field name="barcode_scan" 
                               placeholder="Escanea el código de barras aquí..."
                               attrs="{'invisible': [('state', 'in', ['done', 'cancel'])]}"
                               class="oe_inline"
                               widget="char"/>
                    </group>
                    <group>
                        <label string="💡 Tip: Escanea el código de barras con tu lector USB y el producto se agregará automáticamente"
                               attrs="{'invisible': [('state', 'in', ['done', 'cancel'])]}"/>
                    </group>
                </group>
            </xpath>
            
        </field>
    </record>

</odoo>
